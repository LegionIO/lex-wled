RSpec.describe Legion::Extensions::Wled do
  it "has a version number" do
    expect(Legion::Extensions::Wled::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
